{
  "name": "@zaburoh/ipfy",
  "version": "0.1.0",
  "description": "get global ip address from ipfy.org",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zaburoh",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.3.3"
  }
}
